import { createTheme } from "@mui/material";

const theme1 = createTheme({
    palette: {
        primary: {
            // main: '#5b5291'
            main: '#9370db'
        },
        secondary: {
            main: '#dc004e', // Pink
        },
    }
})

export default theme1;